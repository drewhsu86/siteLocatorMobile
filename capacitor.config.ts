import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	"appId": "io.ionic.starter",
	"appName": "VerticalBridge",
	"webDir": "www",
  plugins: {},
};

export default config;
